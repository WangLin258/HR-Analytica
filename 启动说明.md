# HR Analytica 启动说明

## 启动方式（任选其一）

1. **双击桌面快捷方式**：`HR Analytica.lnk`
2. **双击启动文件**：`start_hr_assistant.bat`
3. **开机自动启动**：已注册 Windows 计划任务 `HRDataAssistant`，登录 Windows 后自动运行

## 启动后会发生什么

1. 检查 8501 端口是否已有 HR Analytica 在运行
2. 若未运行，自动在后台启动 Streamlit 服务
3. 等待服务就绪（最多 30 秒）
4. 自动打开浏览器访问 `http://localhost:8501`

## 手动检查

```powershell
# 查看服务是否监听 8501
netstat -ano | findstr 8501

# 查看启动日志
Get-Content streamlit_log.txt -Tail 20

# 查看错误日志
Get-Content streamlit_err.txt -Tail 20
```

## 常见问题

| 问题 | 解决方法 |
|------|----------|
| 双击快捷方式没反应 | 先删除旧计划任务：`Unregister-ScheduledTask -TaskName HRDataAssistant -Confirm:$false`，再重新注册 |
| 浏览器没有自动打开 | 手动访问 `http://localhost:8501`，然后检查 `streamlit_err.txt` |
| 端口被占用 | 找到占用 8501 的进程并结束：`netstat -ano | findstr 8501`，然后结束对应 PID |

## 文件清单

- `start_hr_assistant.bat` - 双击启动入口
- `start_hr_assistant.ps1` - 启动服务 + 等待 + 打开浏览器
- `.streamlit/config.toml` - Streamlit 配置（隐藏统计弹窗）
- `streamlit_log.txt` - 服务输出日志
- `streamlit_err.txt` - 错误日志
- `app_errors.log` - 应用运行错误日志